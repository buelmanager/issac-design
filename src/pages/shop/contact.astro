---
/**
 * Contact Page
 * Customer service center with contact methods, business hours, quote form, and location
 */

import Layout from '../../layouts/Layout.astro';
import ShopNavbar from '../../components/shop/ShopNavbar.astro';
import Breadcrumb from '../../components/shop/Breadcrumb.astro';
import QuoteForm from '../../components/shop/QuoteForm.astro';
import ShopFooter from '../../components/shop/ShopFooter.astro';
import Toast from '../../components/shop/Toast.astro';
import KakaoFloat from '../../components/shop/KakaoFloat.astro';

const breadcrumbs = [
  { label: '쇼핑몰', href: '/shop' },
  { label: '고객센터' },
];
---

<Layout
  title="고객센터 - issac.design 쇼핑몰"
  description="issac.design 고객센터. 전화, 카카오톡, 이메일로 언제든 문의하세요. 무료 견적 상담 가능."
>
  <script is:inline>document.body.classList.add('preloader-ready');</script>
  <ShopNavbar />
  <Toast />

  <main class="contact-page">
    <!-- Breadcrumb -->
    <div class="contact-page__breadcrumb">
      <Breadcrumb items={breadcrumbs} />
    </div>

    <!-- Hero Section -->
    <section class="contact-hero">
      <div class="contact-hero__container">
        <span class="contact-hero__label">Contact Us</span>
        <h1 class="contact-hero__title">고객센터</h1>
        <p class="contact-hero__subtitle">언제든 편하게 연락주세요</p>
      </div>
    </section>

    <!-- Contact Methods Grid -->
    <section class="contact-methods">
      <div class="contact-methods__container">
        <div class="contact-methods__grid">
          <!-- Phone -->
          <div class="contact-methods__card">
            <div class="contact-methods__icon">
              <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                <path d="M22 16.92v3a2 2 0 01-2.18 2 19.79 19.79 0 01-8.63-3.07 19.5 19.5 0 01-6-6 19.79 19.79 0 01-3.07-8.67A2 2 0 014.11 2h3a2 2 0 012 1.72 12.84 12.84 0 00.7 2.81 2 2 0 01-.45 2.11L8.09 9.91a16 16 0 006 6l1.27-1.27a2 2 0 012.11-.45 12.84 12.84 0 002.81.7A2 2 0 0122 16.92z"/>
              </svg>
            </div>
            <h3 class="contact-methods__card-title">전화 문의</h3>
            <p class="contact-methods__card-value">02-1234-5678</p>
            <p class="contact-methods__card-note">평일 09:00 ~ 18:00</p>
          </div>

          <!-- KakaoTalk -->
          <div class="contact-methods__card">
            <div class="contact-methods__icon">
              <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                <path d="M21 15a2 2 0 01-2 2H7l-4 4V5a2 2 0 012-2h14a2 2 0 012 2z"/>
              </svg>
            </div>
            <h3 class="contact-methods__card-title">카카오톡</h3>
            <p class="contact-methods__card-value">issac_design</p>
            <p class="contact-methods__card-note">24시간 상담 가능</p>
          </div>

          <!-- Email -->
          <div class="contact-methods__card">
            <div class="contact-methods__icon">
              <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                <rect x="2" y="4" width="20" height="16" rx="2"/>
                <path d="M22 7l-10 7L2 7"/>
              </svg>
            </div>
            <h3 class="contact-methods__card-title">이메일</h3>
            <p class="contact-methods__card-value">info@issac.design</p>
            <p class="contact-methods__card-note">24시간 이내 회신</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Business Hours -->
    <section class="contact-hours">
      <div class="contact-hours__container">
        <div class="contact-hours__card">
          <div class="contact-hours__header">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10"/>
              <path d="M12 6v6l4 2"/>
            </svg>
            <h2 class="contact-hours__title">영업시간</h2>
          </div>
          <div class="contact-hours__list">
            <div class="contact-hours__row">
              <span class="contact-hours__day">평일</span>
              <span class="contact-hours__time">09:00 ~ 18:00</span>
            </div>
            <div class="contact-hours__row">
              <span class="contact-hours__day">토요일</span>
              <span class="contact-hours__time">10:00 ~ 15:00</span>
            </div>
            <div class="contact-hours__row contact-hours__row--closed">
              <span class="contact-hours__day">일요일 / 공휴일</span>
              <span class="contact-hours__time">휴무</span>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Quote Form Section -->
    <section class="contact-form" id="quote">
      <div class="contact-form__container">
        <div class="contact-form__header">
          <h2 class="contact-form__title">견적 문의</h2>
          <p class="contact-form__desc">
            아래 양식을 작성해주시면 빠르게 연락드리겠습니다.
          </p>
        </div>
        <QuoteForm />
      </div>
    </section>

    <!-- Location Info -->
    <section class="contact-location">
      <div class="contact-location__container">
        <div class="contact-location__card">
          <div class="contact-location__header">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0118 0z"/>
              <circle cx="12" cy="10" r="3"/>
            </svg>
            <h2 class="contact-location__title">오시는 길</h2>
          </div>
          <p class="contact-location__address">서울특별시 강남구 역삼동 123-45</p>
          <p class="contact-location__note">
            역삼역 3번 출구에서 도보 5분 거리에 위치해 있습니다.
          </p>
        </div>
      </div>
    </section>
  </main>

  <KakaoFloat />
  <ShopFooter />
</Layout>

<style>
  .contact-page {
    position: relative;
    width: 100%;
    min-height: 100vh;
    background: var(--background);
  }

  /* Breadcrumb */
  .contact-page__breadcrumb {
    padding-top: var(--header-height);
  }

  /* ===== Hero Section ===== */
  .contact-hero {
    position: relative;
    width: 100%;
    padding: var(--space-3xl) 0;
    text-align: center;
  }

  .contact-hero__container {
    max-width: var(--container-max);
    margin: 0 auto;
    padding: 0 var(--container-padding);
  }

  .contact-hero__label {
    display: inline-block;
    font-size: var(--font-size-sm);
    font-weight: 500;
    color: var(--primary-light);
    text-transform: uppercase;
    letter-spacing: var(--letter-spacing-wider);
    margin-bottom: var(--space-md);
  }

  .contact-hero__title {
    font-size: var(--font-size-4xl);
    font-weight: 700;
    line-height: var(--line-height-tight);
    letter-spacing: var(--letter-spacing-tight);
    color: var(--text-primary);
    margin-bottom: var(--space-sm);
  }

  .contact-hero__subtitle {
    font-size: var(--font-size-lg);
    color: var(--text-secondary);
    line-height: var(--line-height-relaxed);
  }

  /* ===== Contact Methods Grid ===== */
  .contact-methods {
    position: relative;
    width: 100%;
    padding: var(--space-2xl) 0 var(--space-4xl);
  }

  .contact-methods__container {
    max-width: var(--container-max);
    margin: 0 auto;
    padding: 0 var(--container-padding);
  }

  .contact-methods__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-xl);
  }

  .contact-methods__card {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: var(--space-2xl) var(--space-xl);
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    transition: transform var(--transition-base), border-color var(--transition-base);
  }

  .contact-methods__card:hover {
    transform: translateY(-4px);
    border-color: var(--primary);
  }

  .contact-methods__icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 56px;
    height: 56px;
    background: rgba(26, 77, 46, 0.1);
    border-radius: var(--radius-lg);
    color: var(--primary-light);
    margin-bottom: var(--space-lg);
  }

  .contact-methods__card-title {
    font-size: var(--font-size-sm);
    font-weight: 500;
    color: var(--text-muted);
    text-transform: uppercase;
    letter-spacing: var(--letter-spacing-wide);
    margin-bottom: var(--space-sm);
  }

  .contact-methods__card-value {
    font-size: var(--font-size-xl);
    font-weight: 600;
    color: var(--text-primary);
    margin-bottom: var(--space-xs);
  }

  .contact-methods__card-note {
    font-size: var(--font-size-sm);
    color: var(--text-secondary);
  }

  /* ===== Business Hours ===== */
  .contact-hours {
    position: relative;
    width: 100%;
    padding-bottom: var(--space-4xl);
  }

  .contact-hours__container {
    max-width: 600px;
    margin: 0 auto;
    padding: 0 var(--container-padding);
  }

  .contact-hours__card {
    padding: var(--space-2xl);
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
  }

  .contact-hours__header {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    margin-bottom: var(--space-xl);
    color: var(--text-primary);
  }

  .contact-hours__header svg {
    color: var(--primary-light);
    flex-shrink: 0;
  }

  .contact-hours__title {
    font-size: var(--font-size-xl);
    font-weight: 600;
  }

  .contact-hours__list {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }

  .contact-hours__row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding-bottom: var(--space-md);
    border-bottom: 1px solid var(--border);
  }

  .contact-hours__row:last-child {
    padding-bottom: 0;
    border-bottom: none;
  }

  .contact-hours__day {
    font-size: var(--font-size-base);
    font-weight: 500;
    color: var(--text-primary);
  }

  .contact-hours__time {
    font-size: var(--font-size-base);
    color: var(--text-secondary);
  }

  .contact-hours__row--closed .contact-hours__time {
    color: var(--error);
    font-weight: 500;
  }

  /* ===== Quote Form Section ===== */
  .contact-form {
    position: relative;
    width: 100%;
    padding: var(--space-4xl) 0;
    background: var(--surface);
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
  }

  .contact-form__container {
    max-width: var(--container-max);
    margin: 0 auto;
    padding: 0 var(--container-padding);
  }

  .contact-form__header {
    text-align: center;
    margin-bottom: var(--space-2xl);
  }

  .contact-form__title {
    font-size: var(--font-size-3xl);
    font-weight: 600;
    color: var(--text-primary);
    margin-bottom: var(--space-md);
  }

  .contact-form__desc {
    font-size: var(--font-size-lg);
    color: var(--text-secondary);
  }

  /* ===== Location Info ===== */
  .contact-location {
    position: relative;
    width: 100%;
    padding: var(--space-4xl) 0;
  }

  .contact-location__container {
    max-width: 600px;
    margin: 0 auto;
    padding: 0 var(--container-padding);
  }

  .contact-location__card {
    padding: var(--space-2xl);
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
  }

  .contact-location__header {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    margin-bottom: var(--space-lg);
    color: var(--text-primary);
  }

  .contact-location__header svg {
    color: var(--primary-light);
    flex-shrink: 0;
  }

  .contact-location__title {
    font-size: var(--font-size-xl);
    font-weight: 600;
  }

  .contact-location__address {
    font-size: var(--font-size-lg);
    font-weight: 500;
    color: var(--text-primary);
    margin-bottom: var(--space-sm);
  }

  .contact-location__note {
    font-size: var(--font-size-sm);
    color: var(--text-secondary);
    line-height: var(--line-height-relaxed);
  }

  /* ===== Responsive - Tablet ===== */
  @media (max-width: 1023px) {
    .contact-hero__title {
      font-size: var(--font-size-3xl);
    }

    .contact-methods__grid {
      grid-template-columns: repeat(3, 1fr);
      gap: var(--space-lg);
    }

    .contact-form__title {
      font-size: var(--font-size-2xl);
    }
  }

  /* ===== Responsive - Mobile ===== */
  @media (max-width: 719px) {
    .contact-page__breadcrumb {
      padding-top: var(--header-height-mobile);
    }

    .contact-hero {
      padding: var(--space-2xl) 0;
    }

    .contact-hero__title {
      font-size: var(--font-size-2xl);
    }

    .contact-hero__subtitle {
      font-size: var(--font-size-base);
    }

    .contact-methods {
      padding: var(--space-xl) 0 var(--space-3xl);
    }

    .contact-methods__grid {
      grid-template-columns: 1fr;
      gap: var(--space-md);
    }

    .contact-methods__card {
      padding: var(--space-xl) var(--space-lg);
    }

    .contact-hours {
      padding-bottom: var(--space-3xl);
    }

    .contact-hours__card {
      padding: var(--space-xl);
    }

    .contact-form {
      padding: var(--space-3xl) 0;
    }

    .contact-form__title {
      font-size: var(--font-size-xl);
    }

    .contact-form__desc {
      font-size: var(--font-size-base);
    }

    .contact-location {
      padding: var(--space-3xl) 0;
    }

    .contact-location__card {
      padding: var(--space-xl);
    }

    .contact-location__address {
      font-size: var(--font-size-base);
    }
  }
</style>
